import { State } from "./state.js";

export class MobileState extends State {
    constructor(scene) {
        super(scene);

    }

    animate() {
        super.animate();
    }

    tick() {
        super.tick();
    }
}